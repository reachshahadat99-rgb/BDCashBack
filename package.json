{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'case \"$npm_config_user_agent\" in pnpm/*|npm/*) exit 0 ;; *) echo \"Use pnpm or npm\" >&2; exit 1 ;; esac'",
    "build": "pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@replit/connectors-sdk": "^0.4.1",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
